//wap to calculate area of a square

#include<stdio.h>
int main(){
    int side;
    printf("Enter the side :");
    scanf("%d", &side);
    printf("Area of the sqaure is: %d", side*side);
    return 0;
}